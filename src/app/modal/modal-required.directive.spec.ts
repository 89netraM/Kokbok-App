import { ModalRequiredDirective } from "./modal-required.directive";

describe("ModalRequiredDirective", () => {
	it("should create an instance", () => {
		const directive = new ModalRequiredDirective();
		expect(directive).toBeTruthy();
	});
});
